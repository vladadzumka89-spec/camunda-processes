---
name: check
description: "🔍 Перевірити модулі Odoo перед комітом (маніфести, безпека, структура)"
---

# 🔍 Check — перевірка якості модулів Odoo

Перевіряє змінені модулі на відповідність стандартам проєкту ПЕРЕД комітом.

## Крок 1: Визнач які модулі перевіряти

Якщо `$ARGUMENTS` не порожній — перевіряй вказані модулі.

Інакше — знайди змінені модулі автоматично:
```bash
git diff --name-only HEAD 2>/dev/null; git diff --name-only --cached; git ls-files --others --exclude-standard
```

Витягни унікальні назви модулів з `src/custom/`:
- Файл `src/custom/MODULE_NAME/...` → модуль `MODULE_NAME`
- Ігноруй файли поза `src/custom/`

Якщо змінених модулів немає — скажи: "Немає змінених модулів для перевірки." і заверши.

## Крок 2: Перевірки для кожного модуля

Для кожного знайденого модуля виконай ВСІ перевірки нижче. Збирай результати в список.

### 2.1 Маніфест (`__manifest__.py`)

Прочитай `src/custom/MODULE_NAME/__manifest__.py`.

**Обов'язкові поля:**
- `name` — не порожній рядок
- `version` — формат `19.0.X.X.X` (5 частин через крапку, починається з `19.0`)
- `category` — не порожній
- `depends` — непорожній список
- `license` — має бути присутній (напр. `LGPL-3`)

**Попередження (не блокують, але виводяться):**
- `description` — якщо відсутній, попередити
- `author` — якщо відсутній, попередити
- Якщо `assets` містить шляхи без wildcards для директорій (напр. `module/static/src/file.js` замість `module/static/src/**/*`) — попередити

### 2.2 Безпека доступу

Перевір чи існує `src/custom/MODULE_NAME/security/ir.model.access.csv`.

Знайди всі Python моделі модуля:
```bash
# Шукаємо _name = 'xxx' визначення в Python файлах модуля
```

Для кожної моделі з `_name` (не `_inherit` без `_name`) перевір:
- Чи є рядок з цією моделлю в `ir.model.access.csv`
- Якщо моделі немає у файлі доступу — це **ПОМИЛКА** (модель буде недоступна)

**Виняток:** моделі що наслідують (`_inherit`) без власного `_name` НЕ потребують окремого запису.

### 2.3 Структура Python (`__init__.py`)

Перевір `src/custom/MODULE_NAME/__init__.py` та `src/custom/MODULE_NAME/models/__init__.py`:
- Кожен `.py` файл у директорії `models/` має бути імпортований в `models/__init__.py`
- Кожна піддиректорія з `__init__.py` має бути імпортована в кореневому `__init__.py`
- Директорії `controllers/`, `wizards/` — аналогічно

### 2.4 XML ID конвенція

Перевір XML/CSV файли модуля на наявність `id=` атрибутів:
- XML ID мають починатися з назви модуля або бути без крапки (локальні)
- Якщо є `id="view_something_custom"` — попередити, що краще `id="view_something_extend_FEATURE"`

### 2.5 Префікс `tut_`

Для модулів з префіксом `tut_`:
- Перевір що нові поля в Python моделях мають коментар або docstring
- Перевір що `_name` нових моделей починається з назви модуля або є `_inherit`

## Крок 3: Звіт

Виведи результат у форматі:

> 🔍 **Перевірка модулів завершена**
>
> **MODULE_NAME:**
> - ✅ Маніфест: OK
> - ✅ Безпека: OK (3 моделі, всі мають доступ)
> - ⚠️ Структура: `models/new_file.py` не імпортовано в `__init__.py`
> - ❌ Безпека: модель `tut.new.model` відсутня в `ir.model.access.csv`

Підсумок:
- **Помилки (❌):** блокуючі проблеми які ТРЕБА виправити
- **Попередження (⚠️):** рекомендації які ВАРТО виправити
- **OK (✅):** все добре

Якщо є помилки — запитай через AskUserQuestion:
- "Знайдено проблеми. Хочете щоб я їх виправив?"
- Варіанти: "Так, виправити" / "Ні, лишити як є"

Якщо користувач погодився — виправ проблеми автоматично:
- Додай відсутні імпорти в `__init__.py`
- Додай відсутні записи в `ir.model.access.csv` (з базовими правами: read для всіх, full для admin)
- Виправ формат версії в маніфесті
