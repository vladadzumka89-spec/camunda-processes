---
name: sync
description: "🔄 Синхронізувати локальну копію з remote main (безпечно)"
---

# 🔄 Sync — безпечна синхронізація з remote main

Користувач хоче синхронізувати свою локальну копію з віддаленою гілкою main.

## Крок 1: Покажи поточний стан

Виконай:
```bash
git branch --show-current
git status --short
git log --oneline -5
```

Запам'ятай:
- `CURRENT_BRANCH` — поточна гілка
- `HAS_CHANGES` — чи є незакомічені зміни
- `CURRENT_HEAD` — поточний HEAD коміт

## Крок 2: Автоматично збережи зміни

Якщо `HAS_CHANGES` — автоматично зроби stash БЕЗ питань:

```bash
git stash push -m "auto-stash before /sync ($(date +%Y-%m-%d_%H:%M))"
```

Повідом:
> 💾 Ваші зміни збережено в безпечне місце (stash). Після синхронізації їх можна повернути.

Якщо stash не вдався (наприклад, нові untracked файли) — додай їх:
```bash
git stash push -u -m "auto-stash before /sync ($(date +%Y-%m-%d_%H:%M))"
```

## Крок 3: Попередження

Покажи ЧІТКЕ пояснення **простою мовою**:

> 🔄 **Що зараз станеться:**
> - Я завантажу найновішу версію коду з GitHub
> - Ваша локальна копія стане повністю ідентичною тому що є на сервері
> - Будь-які локальні коміти, які не були відправлені на GitHub — будуть видалені
>
> ℹ️ Ваші незакомічені зміни збережені в stash (якщо були).

Запитай підтвердження через AskUserQuestion:
- "Синхронізувати локальну копію з GitHub?"
- Варіанти: "Так, синхронізувати" / "Ні, скасувати"

**Якщо користувач відмовився:**
Якщо був stash — відновити його:
```bash
git stash pop
```
Скажи "Синхронізацію скасовано. Ваші зміни на місці." і заверши.

## Крок 4: Синхронізація

```bash
git fetch origin main && git checkout main && git reset --hard origin/main && git clean -fd
```

## Крок 5: Покажи що змінилось

Порівняй старий і новий HEAD:
```bash
git log --oneline CURRENT_HEAD..HEAD
```

Якщо є нові коміти — покажи їх простою мовою:
> 📦 **Що нового (N комітів):**
> - `[module_name] Опис зміни`
> - `[module_name] Опис зміни`

Якщо нових комітів немає:
> ✅ Код вже був актуальний, нічого нового.

## Крок 6: Перевірка

```bash
git log --oneline -3
git status
```

Покажи результат:
> ✅ **Синхронізовано!**
> - 📍 Гілка: `main`
> - 📦 Останній коміт: [хеш і повідомлення]
> - 💾 Stash: є збережені зміни (`git stash pop` щоб повернути)

Якщо stash було збережено — нагадай:
> 💡 Якщо хочете повернути ваші попередні зміни — скажіть "поверни зміни зі stash".
