---
name: release
description: "📋 Згенерувати release notes після upstream sync"
---

# 📋 Release Notes — генерація після upstream sync

Згенеруй release notes українською мовою після upstream sync. Цільова аудиторія — звичайні користувачі Odoo (менеджери, бухгалтери, HR), НЕ розробники. Описуй тільки те, що користувач побачить і зможе використати на практиці.

## Крок 1: Знайти sync коміт і базу

```bash
git log --grep='\[sync\]' --format='%H %s' -1
```

Це SYNC_COMMIT. Далі:

```bash
git log SYNC_COMMIT --format='%P' -1
git log SYNC_COMMIT -1 --format='%B'
```

Збережи: SYNC_COMMIT, BASE_COMMIT, дату, enterprise SHA, community SHA.

Якщо sync комітів не знайдено — повідом і зупинись.

## Крок 2: Список змінених модулів + метадані

```bash
# Enterprise модулі
git diff --name-only BASE_COMMIT SYNC_COMMIT -- src/enterprise/ | sed 's|src/enterprise/\([^/]*\)/.*|\1|' | sort -u

# Community модулі
git diff --name-only BASE_COMMIT SYNC_COMMIT -- src/community/ | sed 's|src/community/\([^/]*\)/.*|\1|' | sort -u

# Нові модулі
git diff --diff-filter=A --name-only BASE_COMMIT SYNC_COMMIT -- 'src/enterprise/*/__manifest__.py' 'src/community/*/__manifest__.py'

# Видалені модулі
git diff --diff-filter=D --name-only BASE_COMMIT SYNC_COMMIT -- 'src/enterprise/*/__manifest__.py' 'src/community/*/__manifest__.py'

# Загальна статистика
git diff --stat BASE_COMMIT SYNC_COMMIT -- src/enterprise/ src/community/ ':(exclude)*/i18n/*' ':(exclude)*/tests/*' ':(exclude)*.po' ':(exclude)*.pot' | tail -1
```

Прочитай `__manifest__.py` для кожного зміненого модуля (category, name, summary):

```bash
python3 -c "
import ast, os, sys, json
modules = sys.argv[1:]
result = {}
for mod in modules:
    for base in ['src/enterprise', 'src/community']:
        manifest_path = f'{base}/{mod}/__manifest__.py'
        if os.path.exists(manifest_path):
            with open(manifest_path) as f:
                data = ast.literal_eval(f.read())
            result[mod] = {
                'name': data.get('name', mod),
                'category': data.get('category', 'Other'),
                'summary': data.get('summary', '')
            }
            break
    else:
        result[mod] = {'name': mod, 'category': 'Other', 'summary': ''}
print(json.dumps(result, ensure_ascii=False, indent=2))
" MODULE1 MODULE2 ...
```

Якщо модулів >100 — розбий на батчі по 50.

### Маппінг категорій:
- Accounting → Бухгалтерія
- Sales → Продажі
- Sales/CRM → CRM
- Inventory → Склад
- Purchase → Закупівлі
- Manufacturing → Виробництво
- Human Resources → HR / Управління персоналом
- Project → Проєкти
- Website → Вебсайт
- Marketing → Маркетинг
- Productivity → Продуктивність
- Administration → Адміністрування
- Technical → Технічне
- Discuss → Комунікації
- Point of Sale → Каса (POS)
- Fleet → Автопарк
- Helpdesk → Підтримка
- Sign → Підписи
- Planning → Планування
- Subscriptions → Підписки
- Quality → Якість
- Maintenance → Обслуговування
- Other → Інше

## Крок 3: Паралельний аналіз дифів через субагентів

**ЗАВЖДИ** запускай субагентів паралельно для аналізу дифів. Розбий змінені модулі на групи по категоріях і запусти Task tool з subagent_type="general-purpose" для кожної групи одночасно.

Кожному субагенту передай:
- BASE_COMMIT і SYNC_COMMIT
- Список модулів його групи
- Інструкцію нижче

### Промпт для субагента:

```
Проаналізуй git diff для кожного модуля зі списку і опиши зміни МОВОЮ ЗВИЧАЙНОГО КОРИСТУВАЧА.

Коміти: BASE_COMMIT..SYNC_COMMIT

Модулі: [список]

Для кожного модуля виконай:
git diff BASE_COMMIT SYNC_COMMIT -- src/enterprise/MODULE/ src/community/MODULE/ ':(exclude)*/i18n/*' ':(exclude)*/tests/*' ':(exclude)*.po' ':(exclude)*.pot'

Якщо diff великий — використай --stat і читай тільки ключові змінені файли.

ПРАВИЛА ОПИСУ:
- Пиши ТІЛЬКИ українською
- Описуй з точки зору користувача: "тепер можна...", "покращено...", "додано..."
- ЗАБОРОНЕНО: назви полів, класів, методів, файлів, технічні терміни (ORM, widget, controller, XML ID, ir.rule)
- Описуй те що користувач ПОБАЧИТЬ або ЗМОЖЕ ЗРОБИТИ інакше
- Ігноруй зміни які не впливають на користувацький досвід (рефакторинг, оптимізація коду, зміни тестів)
- Якщо зміни тільки косметичні/технічні — напиши "Дрібні покращення стабільності"
- Максимум 5 пунктів на модуль

Поверни результат у форматі:
### Назва модуля українською
- Опис зміни 1
- Опис зміни 2

Якщо модуль мав тільки дрібні зміни:
### Назва модуля українською
> Дрібні покращення стабільності.
```

### Розбиття на групи:

Запускай **3-6 субагентів паралельно**, розбиваючи модулі за категоріями:
- Група 1: Продажі + CRM + Підписки
- Група 2: Бухгалтерія + Закупівлі
- Група 3: Склад + Виробництво + Якість
- Група 4: HR + Планування + Автопарк
- Група 5: Вебсайт + Маркетинг + POS
- Група 6: Проєкти + Комунікації + Підтримка + решта

Якщо в групі немає змінених модулів — не запускай субагента для неї.

**Community core** (зміни в `src/community/odoo/` та `src/community/addons/base/`): аналізуй окремим субагентом, описуй як "Загальні покращення системи".

## Крок 4: Збір результатів і генерація release notes

Після завершення всіх субагентів, збери їхні результати і згенеруй фінальний Markdown.

### Вимоги до тексту:
- **Мова**: виключно українська
- **Стиль**: як оголошення про оновлення для користувачів. Ніби пишеш пост "Що нового" для клієнтів
- **Тон**: позитивний, практичний — фокус на користі
- **Заборонено**: будь-яка технічна мова, назви моделей/полів/класів/файлів, слова "рефакторинг", "міграція", "API", "endpoint", "widget", "контролер"
- Якщо не можеш описати зміну без технічних термінів — пропусти її або узагальни як "покращення стабільності"

### Структура документу:

```markdown
# Що нового — {{ДАТА}}

> Оновлення системи Odoo · {{ДАТА}}

---

## ✦ Головне в цьому оновленні

- Найважливіша зміна 1 (одне речення, зрозуміле директору)
- Найважливіша зміна 2
- Найважливіша зміна 3

---

## 🆕 Нові можливості
<!-- тільки якщо є нові модулі -->
- **Назва** — що це дає користувачу

---

## 🛒 Продажі
### Назва функціоналу
- Що змінилось для користувача

---

## 💰 Бухгалтерія
### Назва функціоналу
- Що змінилось

---

<!-- інші домени за аналогією -->

## 📊 Статистика оновлення

| | |
|---|---|
| Оновлено модулів | {{N}} |
| Змінено файлів | {{N}} |
```

## Крок 5: Збереження

Збережи через Write tool як `docs/release-notes-YYYY-MM-DD.md` (дата sync коміту).

**Перевір що:**
1. Немає жодного `{{...}}` плейсхолдера — все замінено реальними даними
2. Немає технічних термінів — перечитай і видали якщо пропустив
3. Кожен опис зміни відповідає на питання "що це дає мені як користувачу?"

Повідом користувача де файл і коротко що в ньому.
